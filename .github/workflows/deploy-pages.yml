name: Deploy to GitHub Pages

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    if: >-
      ${{
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.head_branch == 'master'
      }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Prepare static artifact
        run: |
          mkdir -p _site
          cp index.html _site/
          cp -R src _site/src
          mkdir -p _site/data/ephemeris/v2
          cp data/ephemeris/v2/manifest.json _site/data/ephemeris/v2/
          cp -R data/ephemeris/v2/chunks _site/data/ephemeris/v2/chunks
          cp orbital-drift.mp3 _site/
          if [ -f CNAME ]; then cp CNAME _site/; fi
          if [ -f favicon.ico ]; then cp favicon.ico _site/; fi

      - name: Upload static site artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
